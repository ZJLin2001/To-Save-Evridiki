使用方法：
1. 选择载入图像和对应的训练网络 
    无负号手写体对应mynet.m
    有负号手写体对应mynet_handwriting.m
    图标打印体数字对应mynet_print.m
2. 输入上下左右移动惩罚，不能移动则为Inf。选择输出路径最小值或最大值（取决于游戏规则）。
3. 点击“开始解决”。输出面板会输出迷宫图，此时可以选择出发点和结束点[X,Y]坐标。如 3×4 矩阵默认规则下出发点坐标为[1,1],结束点坐标为[3,4]。
4. 右侧会自动弹出可选出发点和结束点坐标。选择合适的出发点坐标和结束点坐标，即可点击“路线打印”，输出路线。假若出现负权回路（死循环），循环50次后会跳出。
